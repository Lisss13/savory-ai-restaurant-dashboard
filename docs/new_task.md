# Техническое задание: Добавление КБЖУ (пищевая ценность) для блюд

## 1. Описание задачи

Реализовать отображение и редактирование пищевой ценности блюд (КБЖУ) в панели администратора ресторана.

**КБЖУ** — это показатели пищевой ценности продукта:
- **К** — Калории (ккал)
- **Б** — Белки (г)
- **Ж** — Жиры (г)
- **У** — Углеводы (г)

---

## 2. Изменения в Backend (уже реализовано)

### 2.1 Модель Dish

В модель `Dish` добавлены новые поля:

```go
type Dish struct {
    // ... существующие поля ...
    Proteins      float64 `json:"proteins"`      // Белки (г)
    Fats          float64 `json:"fats"`          // Жиры (г)
    Carbohydrates float64 `json:"carbohydrates"` // Углеводы (г)
    Calories      float64 `json:"calories"`      // Калории (ккал)
}
```

### 2.2 API Endpoints

Поля КБЖУ доступны во всех эндпоинтах работы с блюдами:

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/dishes/restaurant/:restaurant_id` | Получить блюда ресторана |
| `GET` | `/dishes/category/:restaurant_id` | Получить блюда по категориям |
| `GET` | `/dishes/:id` | Получить блюдо по ID |
| `POST` | `/dishes` | Создать блюдо |
| `PUT` | `/dishes/:id` | Обновить блюдо |

### 2.3 Структура запроса (Create/Update)

```typescript
interface CreateDishRequest {
  restaurant_id: number;      // обязательное
  menuCategoryId: number;   // обязательное
  name: string;               // обязательное
  price: number;              // обязательное
  description?: string;
  image?: string;
  proteins?: number;          // NEW: Белки (г), по умолчанию 0
  fats?: number;              // NEW: Жиры (г), по умолчанию 0
  carbohydrates?: number;     // NEW: Углеводы (г), по умолчанию 0
  calories?: number;          // NEW: Калории (ккал), по умолчанию 0
  ingredients: Ingredient[];  // обязательное
  allergens?: Allergen[];
}
```

### 2.4 Структура ответа

```typescript
interface DishResponse {
  id: number;
  created_at: string;
  restaurant: {
    id: number;
    name: string;
  };
  menu_category: {
    id: number;
    name: string;
  };
  name: string;
  price: number;
  description: string;
  image: string;
  proteins: number;       // NEW
  fats: number;           // NEW
  carbohydrates: number;  // NEW
  calories: number;       // NEW
  ingredients: Ingredient[];
  allergens: Allergen[];
}
```

---

## 3. Требования к Frontend

### 3.1 Форма создания/редактирования блюда

Добавить новую секцию **"Пищевая ценность (КБЖУ)"** в форму создания и редактирования блюда.

#### Расположение в форме

```
Форма блюда:
├── Шаг 1: Основная информация
│   ├── Категория *
│   ├── Название *
│   ├── Цена *
│   ├── Описание
│   ├── Фото
│   └── ☑️ Блюдо дня
│
├── Шаг 2: Пищевая ценность (КБЖУ)  ← NEW SECTION
│   ├── Калории (ккал)
│   ├── Белки (г)
│   ├── Жиры (г)
│   └── Углеводы (г)
│
├── Шаг 3: Ингредиенты
│   └── [Динамический список]
│
└── Шаг 4: Аллергены
    └── [Динамический список]
```

#### UI компонент секции КБЖУ

```
┌─────────────────────────────────────────────────────────────┐
│  Пищевая ценность (на 100г)                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
│  │ Калории     │  │ Белки       │  │ Жиры        │  │ Углеводы    │
│  │ [    320  ] │  │ [   35.5  ] │  │ [   18.2  ] │  │ [    5.0  ] │
│  │    ккал     │  │      г      │  │      г      │  │      г      │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘
│                                                             │
│  💡 Укажите пищевую ценность для информирования гостей      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### Спецификация полей

| Поле | Тип | Placeholder | Min | Max | Шаг | Обязательное |
|------|-----|-------------|-----|-----|-----|--------------|
| `calories` | number | 0 | 0 | 9999 | 1 | Нет |
| `proteins` | number | 0 | 0 | 999 | 0.1 | Нет |
| `fats` | number | 0 | 0 | 999 | 0.1 | Нет |
| `carbohydrates` | number | 0 | 0 | 999 | 0.1 | Нет |

#### Валидация

- Все поля опциональные (по умолчанию 0)
- Только положительные числа или 0
- Допускаются дробные значения (1 знак после запятой)
- При пустом значении отправлять 0

---

### 3.2 Отображение в списке блюд

#### Таблица блюд `/menu/dishes`

Добавить колонку КБЖУ в таблицу (опционально, по желанию пользователя):

| Фото | Название | Категория | Цена | КБЖУ | Блюдо дня | Действия |
|------|----------|-----------|------|------|-----------|----------|
| 🖼️ | Лосось на гриле | Основные | 24.99€ | 320 ккал | ⭐ | ✏️ 🗑️ |

**Формат отображения КБЖУ в таблице:**
- Компактный: только калории (`320 ккал`)
- При наведении (tooltip): полная информация

```
┌────────────────────────┐
│  Пищевая ценность      │
│  ────────────────────  │
│  Калории:    320 ккал  │
│  Белки:      35.5 г    │
│  Жиры:       18.2 г    │
│  Углеводы:   5.0 г     │
└────────────────────────┘
```

---

### 3.3 Карточка блюда (детальный просмотр)

При просмотре блюда отображать блок КБЖУ:

```
┌─────────────────────────────────────────────────┐
│  🍽️ Лосось на гриле                             │
│  Основные блюда • 24.99€                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  [ФОТО БЛЮДА]                                   │
│                                                 │
│  Fresh Atlantic salmon with vegetables          │
│                                                 │
├─────────────────────────────────────────────────┤
│  Пищевая ценность (на 100г)                     │
│  ┌────────┬────────┬────────┬────────┐         │
│  │ 320    │ 35.5   │ 18.2   │ 5.0    │         │
│  │ ккал   │ белки  │ жиры   │ углев. │         │
│  └────────┴────────┴────────┴────────┘         │
├─────────────────────────────────────────────────┤
│  Ингредиенты: ...                               │
│  Аллергены: ...                                 │
└─────────────────────────────────────────────────┘
```

**Условие отображения:**
- Если все значения КБЖУ = 0, блок можно скрыть или показать с пометкой "Не указано"

---

### 3.4 Превью для гостя

В режиме превью карточки блюда (как увидит гость) отображать КБЖУ в компактном виде:

```
┌─────────────────────────┐
│  [ФОТО]                 │
│  Лосось на гриле        │
│  24.99€                 │
│  ─────────────────────  │
│  🔥 320 ккал            │
│  Б: 35.5  Ж: 18.2  У: 5 │
└─────────────────────────┘
```

---

## 4. TypeScript интерфейсы

```typescript
// Обновлённый интерфейс блюда
interface Dish {
  id: number;
  createdAt: string;
  restaurant: {
    id: number;
    name: string;
  };
  menuCategory: {
    id: number;
    name: string;
  };
  name: string;
  price: number;
  description: string;
  image: string;
  // NEW: Пищевая ценность
  proteins: number;
  fats: number;
  carbohydrates: number;
  calories: number;
  // Связанные данные
  ingredients: Ingredient[];
  allergens: Allergen[];
}

// Форма создания/редактирования
interface DishFormData {
  restaurantId: number;
  menuCategoryId: number;
  name: string;
  price: number;
  description: string;
  image: string;
  // NEW: Пищевая ценность
  proteins: number;
  fats: number;
  carbohydrates: number;
  calories: number;
  // Связанные данные
  ingredients: IngredientFormData[];
  allergens: AllergenFormData[];
}

// Компонент отображения КБЖУ
interface NutritionDisplayProps {
  calories: number;
  proteins: number;
  fats: number;
  carbohydrates: number;
  compact?: boolean;  // компактный режим для таблиц
  showEmpty?: boolean; // показывать если всё 0
}
```

---

## 5. API запросы

### 5.1 Создание блюда с КБЖУ

```typescript
const createDish = async (data: DishFormData) => {
  const response = await api.post('/dishes', {
    restaurant_id: data.restaurantId,
    menuCategoryId: data.menuCategoryId,
    name: data.name,
    price: data.price,
    description: data.description,
    image: data.image,
    proteins: data.proteins || 0,
    fats: data.fats || 0,
    carbohydrates: data.carbohydrates || 0,
    calories: data.calories || 0,
    ingredients: data.ingredients,
    allergens: data.allergens,
  });
  return response.data;
};
```

### 5.2 Обновление блюда с КБЖУ

```typescript
const updateDish = async (id: number, data: DishFormData) => {
  const response = await api.put(`/dishes/${id}`, {
    restaurant_id: data.restaurantId,
      menuCategoryId: data.menuCategoryId,
    name: data.name,
    price: data.price,
    description: data.description,
    image: data.image,
    proteins: data.proteins || 0,
    fats: data.fats || 0,
    carbohydrates: data.carbohydrates || 0,
    calories: data.calories || 0,
    ingredients: data.ingredients,
    allergens: data.allergens,
  });
  return response.data;
};
```

---

## 6. Компоненты React

### 6.1 NutritionInput — форма ввода КБЖУ

```tsx
interface NutritionInputProps {
  value: {
    calories: number;
    proteins: number;
    fats: number;
    carbohydrates: number;
  };
  onChange: (value: NutritionInputProps['value']) => void;
}

const NutritionInput: React.FC<NutritionInputProps> = ({ value, onChange }) => {
  // Реализация формы с 4 полями ввода
  // Поля: Калории, Белки, Жиры, Углеводы
  // Подсказка внизу
};
```

### 6.2 NutritionBadge — компактное отображение

```tsx
interface NutritionBadgeProps {
  calories: number;
  proteins: number;
  fats: number;
  carbohydrates: number;
  showTooltip?: boolean;
}

const NutritionBadge: React.FC<NutritionBadgeProps> = (props) => {
  // Компактное отображение калорий
  // При наведении — tooltip с полной информацией
};
```

### 6.3 NutritionCard — полное отображение

```tsx
interface NutritionCardProps {
  calories: number;
  proteins: number;
  fats: number;
  carbohydrates: number;
  title?: string;
}

const NutritionCard: React.FC<NutritionCardProps> = (props) => {
  // Карточка с 4 значениями
  // Используется в детальном просмотре блюда
};
```

---

## 7. Локализация

### Русский (ru)

```json
{
  "nutrition": {
    "title": "Пищевая ценность",
    "per100g": "на 100г",
    "calories": "Калории",
    "proteins": "Белки",
    "fats": "Жиры",
    "carbohydrates": "Углеводы",
    "kcal": "ккал",
    "gram": "г",
    "hint": "Укажите пищевую ценность для информирования гостей",
    "notSpecified": "Не указано"
  }
}
```

### English (en)

```json
{
  "nutrition": {
    "title": "Nutrition Facts",
    "per100g": "per 100g",
    "calories": "Calories",
    "proteins": "Protein",
    "fats": "Fat",
    "carbohydrates": "Carbs",
    "kcal": "kcal",
    "gram": "g",
    "hint": "Specify nutritional value to inform guests",
    "notSpecified": "Not specified"
  }
}
```

---

## 8. Тестовые сценарии

### 8.1 Создание блюда с КБЖУ

1. Открыть форму создания блюда
2. Заполнить обязательные поля
3. Перейти в секцию "Пищевая ценность"
4. Ввести значения: калории=320, белки=35.5, жиры=18.2, углеводы=5
5. Сохранить блюдо
6. **Ожидаемый результат:** Блюдо создано, значения КБЖУ сохранены

### 8.2 Создание блюда без КБЖУ

1. Создать блюдо без заполнения КБЖУ
2. **Ожидаемый результат:** Блюдо создано, все значения КБЖУ = 0

### 8.3 Редактирование КБЖУ

1. Открыть существующее блюдо на редактирование
2. Изменить значения КБЖУ
3. Сохранить
4. **Ожидаемый результат:** Новые значения сохранены

### 8.4 Отображение в списке

1. Открыть список блюд
2. **Ожидаемый результат:** Видны калории, при наведении — полная информация

### 8.5 Превью для гостя

1. Открыть превью блюда
2. **Ожидаемый результат:** КБЖУ отображается в компактном виде

---

## 9. Приоритет задач

| # | Задача | Приоритет |
|---|--------|-----------|
| 1 | Форма ввода КБЖУ (создание/редактирование) | Высокий |
| 2 | Отображение в детальном просмотре блюда | Высокий |
| 3 | Отображение в таблице списка блюд | Средний |
| 4 | Превью карточки для гостя | Средний |
| 5 | Локализация | Низкий |

---

---

*Документ создан: Ноябрь 2025*
*Версия API: 1.0*
